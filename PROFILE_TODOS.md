# Profile TODOs

Tracks external links for the homepage. Completed items are wired into
`src/data/home.ts`.

## Done

- [x] Add contact email (`contact.email` in `src/data/home.ts`)
- [x] Add LinkedIn link (`contact.linkedin` in `src/data/home.ts`)
- [x] Add resume link (`resumeUrl` → `public/CV/Hardik-Monga-Resume.pdf`)

## Remaining

- [ ] Add Medium link (for a Writing / experiments section)
- [ ] Add article links (populate `writingLinks` in `src/data/home.ts`)

When `writingLinks` has entries, add the Writing section to the homepage.
